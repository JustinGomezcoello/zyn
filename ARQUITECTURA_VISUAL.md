# 🏗️ ARQUITECTURA DE ZYN

```
┌─────────────────────────────────────────────────────────────────────┐
│                        🌐 USUARIO (NAVEGADOR)                        │
│                     http://localhost:5173                            │
└─────────────────────────────┬───────────────────────────────────────┘
                              │
                              │ HTTP/HTTPS
                              │
┌─────────────────────────────▼───────────────────────────────────────┐
│                    📱 FRONTEND - React + TypeScript                  │
│                           (zyn-cloud-app/)                           │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌────────────────┐  ┌────────────────┐  ┌───────────────┐        │
│  │  LoginPage     │  │  ComprasPage   │  │ ReportesPage  │        │
│  │  RegisterPage  │  │  OrdenCompra   │  │ PrestamosPage │        │
│  └────────┬───────┘  └────────┬───────┘  └───────┬───────┘        │
│           │                   │                   │                 │
│           └───────────────────┼───────────────────┘                 │
│                               │                                     │
│  ┌────────────────────────────▼──────────────────────────┐         │
│  │          🔐 AuthContext (useAuth)                      │         │
│  │   - user, session, loading                             │         │
│  │   - signIn(), signUp(), signOut()                      │         │
│  └────────────────────────┬───────────────────────────────┘         │
│                           │                                         │
│  ┌────────────────────────▼──────────────────────────┐             │
│  │        📚 Business Logic (businessLogic.ts)        │             │
│  │   - calcularIVA(), costoSinIVA()                   │             │
│  │   - calcularComisiones(), calcularUtilidad()       │             │
│  └────────────────────────┬───────────────────────────┘             │
│                           │                                         │
│  ┌────────────────────────▼──────────────────────────┐             │
│  │       🔌 Supabase Client (supabase.ts)             │             │
│  │   createClient(URL, ANON_KEY)                      │             │
│  └────────────────────────┬───────────────────────────┘             │
│                           │                                         │
└───────────────────────────┼─────────────────────────────────────────┘
                            │
                            │ PostgreSQL Protocol
                            │ (con RLS habilitado)
                            │
┌───────────────────────────▼─────────────────────────────────────────┐
│                    ☁️ BACKEND - SUPABASE                             │
│            https://sozrldnxlhepbhcmpcfp.supabase.co                 │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                 🔐 AUTHENTICATION (Auth)                     │   │
│  │  - auth.users (tabla de usuarios)                           │   │
│  │  - JWT Tokens                                               │   │
│  │  - auth.uid() para RLS                                      │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │              💾 DATABASE (PostgreSQL)                        │   │
│  │                                                              │   │
│  │  ┌──────────────────┐  ┌──────────────────┐               │   │
│  │  │   productos      │  │ inventario_      │  ✅ CREADA    │   │
│  │  │   - id           │  │    usuario       │  CON MIGRACIÓN│   │
│  │  │   - user_id      │  │   - id           │               │   │
│  │  │   - CodigoProd   │  │   - user_id      │               │   │
│  │  │   - NombreProd   │  │   - CodigoProd   │               │   │
│  │  │   - CostoConIVA  │  │   - CantInicial  │               │   │
│  │  │   - IVA          │  │   - CantVendida  │               │   │
│  │  └──────────────────┘  └──────────────────┘               │   │
│  │                                                              │   │
│  │  ┌──────────────────┐  ┌──────────────────┐               │   │
│  │  │   compras        │  │ orden_compra     │               │   │
│  │  │   - id           │  │   - id           │               │   │
│  │  │   - user_id      │  │   - user_id      │               │   │
│  │  │   - FechaCompra  │  │   - NumOrden     │               │   │
│  │  │   - CodigoProd   │  │   - Cliente      │               │   │
│  │  │   - Cantidad     │  │   - CodigoProd   │               │   │
│  │  │   - CostoConIVA  │  │   - CantVendida  │               │   │
│  │  └──────────────────┘  └──────────────────┘               │   │
│  │                                                              │   │
│  │  ┌──────────────────┐  ┌──────────────────┐               │   │
│  │  │ cuentas_cobrar   │  │ cuentas_pagar_   │               │   │
│  │  │   - id           │  │   consultor      │               │   │
│  │  │   - user_id      │  │   - id           │               │   │
│  │  │   - NumOrden     │  │   - user_id      │               │   │
│  │  │   - Cliente      │  │   - NumOrden     │               │   │
│  │  │   - Valores...   │  │   - Comision...  │               │   │
│  │  └──────────────────┘  └──────────────────┘               │   │
│  │                                                              │   │
│  │  ┌──────────────────┐  ┌──────────────────┐               │   │
│  │  │   prestamos      │  │  devoluciones    │               │   │
│  │  │   - id           │  │   - id           │               │   │
│  │  │   - user_id      │  │   - user_id      │               │   │
│  │  │   - CodigoProd   │  │   - IdPrestamo   │               │   │
│  │  │   - Cantidad...  │  │   - Cantidad...  │               │   │
│  │  └──────────────────┘  └──────────────────┘               │   │
│  │                                                              │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │           🔒 ROW LEVEL SECURITY (RLS)                        │   │
│  │                                                              │   │
│  │  Policy: "usuarios_ven_sus_productos"                       │   │
│  │  USING (auth.uid() = user_id)                               │   │
│  │                                                              │   │
│  │  Policy: "usuarios_ven_sus_compras"                         │   │
│  │  USING (auth.uid() = user_id)                               │   │
│  │                                                              │   │
│  │  Policy: "usuarios_ven_su_inventario"  ✅ NUEVA             │   │
│  │  USING (auth.uid() = user_id)                               │   │
│  │                                                              │   │
│  │  ℹ️ Cada usuario SOLO ve sus propios datos                  │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════
                        🔄 FLUJO DE DATOS
═══════════════════════════════════════════════════════════════════════

1. REGISTRO/LOGIN:
   Usuario → LoginPage → AuthContext.signIn() 
   → Supabase Auth → JWT Token → Session guardada

2. CONSULTA DE DATOS (con RLS):
   Usuario autenticado → ComprasPage 
   → supabase.from('compras').select()
   → RLS verifica: auth.uid() = user_id ✅
   → Retorna SOLO los datos del usuario

3. INSERTAR DATOS:
   Usuario → ComprasPage (formulario)
   → supabase.from('compras').insert({ user_id: user.id, ... })
   → RLS verifica: auth.uid() = user_id ✅
   → Datos guardados

4. CÁLCULOS:
   Datos de DB → businessLogic.calcularIVA()
   → Cálculo con Decimal.js (precisión)
   → Resultado mostrado en UI


═══════════════════════════════════════════════════════════════════════
                        🔐 SEGURIDAD
═══════════════════════════════════════════════════════════════════════

✅ RLS habilitado en TODAS las tablas
✅ Políticas configuradas para auth.uid() = user_id
✅ ANON_KEY en .env.local (solo lectura pública)
✅ JWT Tokens con expiración automática
✅ HTTPS en producción
✅ Passwords hasheados por Supabase

⚠️ NO INCLUIR en repositorio público:
   - .env.local
   - Service Role Key (si la tienes)


═══════════════════════════════════════════════════════════════════════
                    📦 TECNOLOGÍAS USADAS
═══════════════════════════════════════════════════════════════════════

Frontend:
  - React 19
  - TypeScript 5.9
  - Vite 7.3
  - React Router 7.13
  - Decimal.js 10.6 (cálculos precisos)
  - Lucide Icons
  - date-fns 4.1

Backend:
  - Supabase (PostgreSQL + Auth + Storage)
  - Row Level Security (RLS)
  - PostgreSQL 15

Build & Deploy:
  - npm/pnpm
  - Vercel / Netlify (recomendado)


═══════════════════════════════════════════════════════════════════════
                    ✅ ESTADO DEL PROYECTO
═══════════════════════════════════════════════════════════════════════

┌───────────────────────────────┬──────────┬─────────────────┐
│ Componente                    │ Estado   │ Comentario      │
├───────────────────────────────┼──────────┼─────────────────┤
│ Configuración Supabase        │ ✅ OK    │ Funcionando     │
│ Autenticación                 │ ✅ OK    │ Funcionando     │
│ RLS y Políticas              │ ✅ OK    │ Funcionando     │
│ Tabla inventario_usuario      │ ⚠️ FALTA │ Migración lista │
│ Módulo Compras               │ 🟡 PARCIAL│ Después migr.  │
│ Módulo Órdenes               │ 🟡 PARCIAL│ Después migr.  │
│ Módulo Cuentas Cobrar        │ ✅ OK    │ Funcionando     │
│ Módulo Cuentas Pagar         │ ✅ OK    │ Funcionando     │
│ Módulo Préstamos             │ ✅ OK    │ Funcionando     │
│ Módulo Reportes              │ 🟡 PARCIAL│ Después migr.  │
└───────────────────────────────┴──────────┴─────────────────┘

Leyenda:
  ✅ OK       = Funciona correctamente
  🟡 PARCIAL = Funciona después de ejecutar migración
  ⚠️ FALTA    = Requiere acción
  ❌ ERROR   = No funciona (ninguno encontrado)


═══════════════════════════════════════════════════════════════════════
                    🎯 PRÓXIMOS PASOS
═══════════════════════════════════════════════════════════════════════

1. Ejecutar migración 002_create_inventario_usuario.sql
2. Probar todas las funcionalidades
3. Decidir estrategia para productos (usuario vs global)
4. Deploy a producción (Vercel recomendado)

═══════════════════════════════════════════════════════════════════════
```

## 📊 Comparación: Antes vs Después de la Migración

### ANTES (Estado Actual)
```
❌ ComprasPage.registrarCompra() → Error: inventario_usuario no existe
❌ OrdenCompraPage.actualizarInventario() → Error: inventario_usuario no existe
❌ ReportesPage.reporteInventario() → Error: inventario_usuario no existe
```

### DESPUÉS (Con Migración Aplicada)
```
✅ ComprasPage.registrarCompra() → Compra registrada + inventario actualizado
✅ OrdenCompraPage.actualizarInventario() → Inventario reducido correctamente
✅ ReportesPage.reporteInventario() → Muestra inventario actual
```

---

## 🔍 Resumen Visual de Permisos RLS

```
┌──────────────────────────────────────────────────────────────┐
│                   🔐 PERMISOS POR TABLA                       │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  Usuario A (UUID: xxx-111)                                  │
│  ├─ productos        → Solo ve productos con user_id=111   │
│  ├─ compras          → Solo ve compras con user_id=111     │
│  ├─ inventario_user  → Solo ve inventario con user_id=111  │
│  ├─ orden_compra     → Solo ve órdenes con user_id=111     │
│  └─ ...              → Mismo patrón para todas las tablas   │
│                                                              │
│  Usuario B (UUID: yyy-222)                                  │
│  ├─ productos        → Solo ve productos con user_id=222   │
│  ├─ compras          → Solo ve compras con user_id=222     │
│  └─ ...              → Completamente aislado de Usuario A   │
│                                                              │
│  ℹ️ Esto se logra con:                                      │
│     POLICY USING (auth.uid() = user_id)                     │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

**Creado**: 20 de Febrero, 2026  
**Proyecto**: ZYN v1.0  
**Framework**: React + TypeScript + Supabase
